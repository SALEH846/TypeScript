// A simple variable that accepts only string as its value
let greetings: string = "Hello, My name is Muhammad Saleh";

// cannot do that
// greetings = 22;

// A simple variable that accepts only number as its value
// let age: number = 22;
let age = 22; // both are same
// if type is not provided then TypeScript automatically infers the type from the value

// A simple variable that accepts only boolean as its value
let married: boolean = false;

console.log(greetings);
